## Implementation Defaults

- Prefer semantic HTML and CSS in `static/css/default.css`.
- Avoid external font or icon dependencies unless there is a clear need.
- Validate the result with `zola build` and Playwright screenshots on desktop and mobile.
