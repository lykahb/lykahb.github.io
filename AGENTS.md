# Agent Instructions

This is a Zola static site.

## Run Locally

Build the site:

```sh
zola build
```

Serve with live reload:

```sh
zola serve --interface 127.0.0.1 --port 1111 --force
```

The generated site is written to `docs/`, as configured in `config.toml`.
